# ── Frontend (Vercel → Project → Settings → Environment Variables) ──────────
# These are the ONLY variables the browser bundle sees. Never put secrets in
# VITE_-prefixed vars — they are public by definition.
VITE_SUPABASE_URL=https://uldtyqzchwkbitgnworq.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_1K95Cp1yRpwvqsK1tB58hw_tbk01Aia

# ── Server secrets (Supabase → Project → Edge Functions → Secrets) ──────────
# Set with: supabase secrets set KEY=value  (or dashboard → Edge Functions → Secrets)
# NEVER commit real values. NEVER add these to Vercel — the frontend must not
# hold them.
#
# Xero custom connection (developer.xero.com → New app → Custom connection)
XERO_CLIENT_ID=
XERO_CLIENT_SECRET=
#
# Anthropic API (console.anthropic.com → API keys)
ANTHROPIC_API_KEY=
#
# Provided automatically inside edge functions by Supabase — no action needed:
# SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
